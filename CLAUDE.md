# GED RLA Dungeon — กฎการทำงานสำหรับ Claude Code

> ไฟล์นี้คือกฎที่ Claude Code ต้องปฏิบัติตามตลอดการสร้างเกมนี้
> อ่านไฟล์นี้คู่กับ GED_RLA_Dungeon_GAME_DESIGN.md เสมอ

---

## กฎข้อที่ 1 — ห้าม Hard-code (สำคัญที่สุด)

เกมนี้จะมีการปรับแก้ค่าต่างๆ บ่อย ดังนั้น:

- **ค่าตัวเลขทั้งหมดต้องอยู่ในไฟล์ config/constants แยก** ห้ามเขียนตัวเลขฝังในโค้ดกระจายไปทั่ว
- ค่าที่ต้องอยู่ใน config เช่น:
  - interval steps (1, 3, 7, 14, 30)
  - weight ของแต่ละสถานะ (New=1.0, Learning=3.0/2.0, Relearning=2.5, Mastered=0.1, FastTrack=10.0)
  - คะแนนแต่ละ rank (10, 20, 35, 55, 80, 120)
  - combo multiplier (1.2, 1.5, 2.0, 3.0)
  - pressure thresholds ของ new card gate (5, 10, 20)
  - fast track unlock thresholds (30, 25, 20, 15, 10)
  - learning steps count (3 ครั้ง)
  - HP เริ่มต้น (5)
  - room ต่อ floor (10)
  - effect duration (1.2 วินาที)
- เขียนโค้ดให้ **แยกส่วน (modular)** แต่ละระบบอยู่ไฟล์ของตัวเอง แก้ที่เดียวไม่กระทบที่อื่น
- ใช้ชื่อตัวแปร/ฟังก์ชันที่สื่อความหมาย อ่านแล้วเข้าใจ
- หลีกเลี่ยงการเขียน logic ซ้ำ — ถ้าใช้หลายที่ให้แยกเป็น function

---

## กฎข้อที่ 2 — อธิบายก่อนลงมือทำเสมอ

ทุกครั้งก่อนจะทำอะไร (สร้างไฟล์ เขียนโค้ด รัน command ติดตั้ง package):

1. **บอกก่อนว่ากำลังจะทำอะไร** และทำไปทำไม
2. ถ้าเป็น command ที่ฉันต้องรันเอง ให้บอกว่า command นี้ทำอะไร
3. **รอให้เข้าใจก่อน** แล้วค่อยลงมือ
4. หลังทำเสร็จ สรุปสั้นๆ ว่าทำอะไรไป

เหตุผล: ฉันไม่มี coding background กำลังเรียนรู้ไปด้วย ต้องเข้าใจทุกขั้นตอน ไม่ใช่แค่ได้โค้ดมา

---

## กฎข้อที่ 3 — ถามเมื่อไม่แน่ใจ

- ถ้ามีหลายวิธีทำและแต่ละวิธีมีข้อดีข้อเสียต่างกัน → เสนอตัวเลือกให้ฉันเลือก อย่าตัดสินใจเอง
- ถ้าเจอสิ่งที่ขัดกับ GAME_DESIGN.md → หยุดถามก่อน อย่าเดา
- ถ้าฉันสั่งอะไรที่อาจขัดกับระบบที่ออกแบบไว้ → เตือนฉันก่อน

---

## กฎข้อที่ 4 — ทำทีละขั้นตามแผน

- ทำตามลำดับใน GAME_DESIGN.md หมวด 17 (Phase 1)
- **อย่าทำหลายอย่างพร้อมกันจนฉันตามไม่ทัน** ทำทีละส่วน ให้ฉันเข้าใจและทดสอบได้
- เน้นให้แต่ละส่วนทำงานได้จริงก่อนไปส่วนถัดไป
- ยังไม่ต้องทำส่วนที่อยู่ใน Deferred (หมวด 18)

---

## กฎข้อที่ 5 — เตรียมโครงสร้างสำหรับอนาคต

แม้ Phase 1 ยังไม่ทำ ecosystem แต่ต้องเตรียมไว้ (ดู GAME_DESIGN.md หมวด 16):

- เก็บ `userId` ทุก record (ใช้ "local_user" ไปก่อน)
- ทุกคำมี `tags` array และ wordStats มี `source`
- `translations` เป็น array เสมอ แม้มีความหมายเดียว
- เตรียม export function ไว้

อย่าออกแบบโครงสร้างที่จะต้อง refactor ใหญ่ตอนเพิ่มเกมที่ 2

---

## กฎข้อที่ 6 — ภาษาและการสื่อสาร

- อธิบายเป็นภาษาไทย
- เขียน comment ในโค้ดเป็นภาษาไทยได้ (ช่วยให้ฉันเข้าใจ)
- ใช้ศัพท์เทคนิคได้ แต่ช่วยอธิบายสั้นๆ ถ้าเป็นคำที่ฉันอาจไม่รู้

---

## กฎข้อที่ 7 — เมื่อแก้ไขหรือ debug

- ก่อนแก้ bug อธิบายว่า bug เกิดจากอะไร
- แก้แล้วบอกว่าแก้ตรงไหน เปลี่ยนอะไร
- อย่าแก้หลายไฟล์พร้อมกันโดยไม่บอก

---

## สรุป — สิ่งที่ฉันคาดหวังจาก Claude Code

1. ไม่ hard-code — ค่าทุกอย่างแก้ได้ง่ายจาก config
2. อธิบายทุกขั้นตอนก่อนทำ ฉันต้องเข้าใจ
3. ถามเมื่อไม่แน่ใจ ไม่เดา
4. ทำทีละขั้นตามแผน ไม่รวบรัด
5. เตรียมโครงสร้างเผื่ออนาคต
6. สื่อสารภาษาไทย เข้าใจง่าย

> ถ้าทำตามกฎเหล่านี้ เราจะสร้างเกมนี้ออกมาได้ดีและฉันจะเรียนรู้ไปด้วยพร้อมกัน
